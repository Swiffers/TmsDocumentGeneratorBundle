<h1>{% trans %}Documents list{% endtrans %}</h1>

<table class="table table-striped table-condensed table-hover">
    <thead>
        <tr>
            <th>{% trans %}Name{% endtrans %}</th>
            <th>{% trans %}Created at{% endtrans %}</th>
            <th>{% trans %}Actions{% endtrans %}</th>
        </tr>
    </thead>
    <tbody>

    {% if documents|length == 0 %}
        <tr><td colspan="3">{% trans %}No document{% endtrans %}</td></tr>
    {% else %}
        {% for document in documents %}
            <tr>
                <td>{{ document.name }}</a></td>
                <td>{{ document.createdAt|date('Y-m-d') }}</td>
                <td>
                    <a href="{{ path('tms_documentgenerator_document_edit', { 'id': document.id }) }}" class="btn btn-default btn-sm">
                        {% trans %}Edit{% endtrans %}
                    </a>
                </td>
            </tr>
        {% endfor %}
    {% endif %}
    </tbody>
</table>

<ul class="actions">
    <li>
        <a href="{{ path('tms_documentgenerator_document_create') }}" class="btn btn-default btn-sm">
            {% trans %}Create a new document{% endtrans %}
        </a>
    </li>
</ul>